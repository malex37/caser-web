export type SessionToken = {
  username: string;
  sessionId: string;
  active: boolean;
  cognitoCred: {
    secretKey: string;
    accessKeyId: string;
    sessionToken: string;
    expiration: number;
    refreshToken: string;
  };
  webId: string;
}
